---
import Badge from '../ui/badge.astro';

interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

{
  tags.length > 0 && (
    <div class="flex flex-wrap gap-2">
      {tags.map((tag) => (
        <Badge href={`/blog/tags/${tag}`}>{tag}</Badge>
      ))}
    </div>
  )
}
